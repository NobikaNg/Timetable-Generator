def clear_auto_schedule():
    """
    This method is used to clear all `teacher_id` except those end with `"as"` in every `session`'s `on_duty_teachers` list.
    """
    return

def token_distribution():
    return

def manural_distribution():
    return